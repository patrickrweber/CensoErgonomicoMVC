@{

}
<section>
    <div class="container p-2 mb-5">
        <div class="mt-3">
            @await Html.PartialAsync("_MessageResult")
        </div>
        <div class="row row-cols-1 row-cols-md-3">
            <a id="reg-colaborador" class="col cardi">
                <div class="m-1 p-2 rounded" style="max-width: 400px;">
                    <div class="row g-0">
                        <div class="col-md-3">
                            <img src="/img/employers.png" class="img-fluid rounded-start img-top" alt="...">
                        </div>
                        <div class="col-md-8">
                            <div class="card-body pt-1">
                                <h3 class="card-title">Colaborador</h3>
                                <small class="card-text">Cadastre um novo colaborador.</small>
                            </div>
                        </div>
                    </div>
                </div>
            </a>
            <a id="reg-funcao" class="col cardi">
                <div class="m-1 p-2 rounded" style="max-width: 400px;">
                    <div class="row g-0">
                        <div class="col-md-3">
                            <img src="/img/activity.png" class="img-fluid rounded-start img-top" alt="...">
                        </div>
                        <div class="col-md-8">
                            <div class="card-body pt-1">
                                <h3 class="card-title">Função</h3>
                                <small class="card-text">Cadatre uma nova função no sistema.</small>
                            </div>
                        </div>
                    </div>
                </div>
            </a>
            <a id="reg-setor" class="col cardi">
                <div class="m-1 p-2 rounded" style="max-width: 400px;">
                    <div class="row g-0">
                        <div class="col-md-3">
                            <img src="/img/people.png" class="img-fluid rounded-start img-top" alt="...">
                        </div>
                        <div class="col-md-8">
                            <div class="card-body pt-1">
                                <h3 class="card-title">Setor</h3>
                                <small class="card-text">Cadastre um novo setor da empresa.</small>
                            </div>
                        </div>
                    </div>
                </div>
            </a>
        </div>
    </div>
    <hr />
    <div class="container bd" id="register-area"></div>
</section>

@section Scripts {
    <script>
        function reload_js(src) {
            $('script[src="' + src + '"]').remove();
            $('<script>').attr('src', src).appendTo('body');
        }
    </script>
    <script>
        $('#reg-colaborador').click((e) => {
            $.ajax({
                url: `@Url.Action("Index", "Colaborador")`,
                success: (resp) => {
                    $('#register-area').html(resp);
                }
            });
        });
        $('#reg-funcao').click(() => {
            $.ajax({
                url: `@Url.Action("Index", "Funcao")`,
                success: (resp) => {
                    $('#register-area').html(resp);
                }
            })
        });
        $('#reg-setor').click(() => {
            $.ajax({
                url: `@Url.Action("Index", "Setor")`,
                success: (resp) => {
                    $('#register-area').html(resp);
                }
            })
        });
    </script>
}
